# Backend API - Система анализа данных предприятий Москвы

Мощный и масштабируемый backend для системы сбора, консолидации, анализа и визуализации данных предприятий Москвы.

## 🚀 Технологический стек

- **FastAPI** - современный, быстрый веб-фреймворк для создания API
- **PostgreSQL** - реляционная база данных для хранения данных предприятий
- **SQLAlchemy** - ORM для работы с базой данных
- **Pandas** - библиотека для обработки и анализа данных (ETL)
- **Pydantic** - валидация данных и сериализация
- **ReportLab** - генерация PDF отчётов
- **XlsxWriter** - экспорт в Excel формат

## 📁 Структура проекта

```
backend/
├── app/
│   ├── __init__.py
│   └── schemas.py          # Pydantic схемы для API
├── config/
│   └── settings.py         # Настройки приложения
├── models/
│   ├── __init__.py
│   └── enterprise.py       # SQLAlchemy модели
├── routes/
│   ├── __init__.py
│   ├── enterprises.py      # API эндпоинты для предприятий
│   ├── upload.py          # API для загрузки файлов
│   └── export.py          # API для экспорта данных
├── services/
│   ├── __init__.py
│   ├── data_processing.py  # ETL сервис для обработки данных
│   ├── analytics.py        # Аналитические функции
│   └── export.py          # Сервис экспорта данных
├── uploads/               # Временные загруженные файлы
├── main.py               # Главное приложение FastAPI
├── requirements.txt      # Python зависимости
├── Dockerfile           # Docker контейнер
├── docker-compose.yml   # Docker Compose конфигурация
└── .env.example        # Пример переменных окружения
```

## 🛠 Установка и запуск

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/BortonGo/Hakaton.git
cd Hakaton/backend
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

5. **Запустите PostgreSQL** (или используйте Docker)

6. **Запустите приложение:**
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### Docker запуск

1. **Запустите с помощью Docker Compose:**
```bash
docker-compose up --build
```

Это запустит:
- Backend API на http://localhost:8000
- PostgreSQL на порту 5432
- Redis на порту 6379

## 📚 API Документация

После запуска приложения, интерактивная документация доступна по адресам:

- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

## 🔗 Основные API эндпоинты

### 📊 Предприятия и аналитика

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/v1/enterprises` | Получить список предприятий с фильтрацией |
| `GET` | `/api/v1/aggregates` | Агрегированные данные по отраслям и регионам |
| `GET` | `/api/v1/quality` | Статистика качества данных |
| `GET` | `/api/v1/trends/monthly` | Тренды по месяцам |
| `GET` | `/api/v1/top/{by}` | Топ предприятий по выручке/сотрудникам |
| `GET` | `/api/v1/search` | Поиск предприятий |
| `GET` | `/api/v1/constants` | Константы системы (регионы, отрасли) |

### 📤 Загрузка данных

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/api/v1/upload` | Загрузка файла CSV/Excel |
| `GET` | `/api/v1/upload/{id}/status` | Статус обработки файла |
| `GET` | `/api/v1/uploads` | Список всех загрузок |
| `DELETE` | `/api/v1/upload/{id}` | Удаление загрузки |

### 📥 Экспорт данных

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/v1/export/csv` | Экспорт предприятий в CSV |
| `GET` | `/api/v1/export/excel` | Экспорт предприятий в Excel |
| `GET` | `/api/v1/export/report/pdf` | Генерация PDF отчёта |
| `GET` | `/api/v1/export/aggregates/excel` | Экспорт аналитики в Excel |

## 🔧 Функциональность

### 📁 Загрузка и обработка файлов

- Поддержка форматов: **CSV, XLSX, XLS**
- Автоматическая валидация данных
- ETL процессы: очистка, стандартизация, дедупликация
- Асинхронная обработка больших файлов
- Детальное логирование ошибок

### 🧹 ETL (Extract, Transform, Load)

**Очистка данных:**
- Удаление лишних пробелов и символов
- Стандартизация названий отраслей и регионов
- Приведение числовых значений к правильному формату
- Валидация email адресов и телефонов
- Удаление дубликатов

**Валидация:**
- Проверка обязательных полей
- Валидация отраслей по справочнику
- Проверка регионов Москвы
- Контроль диапазонов числовых значений

### 📊 Аналитика и агрегация

- Статистика по отраслям и регионам
- Временные тренды и динамика
- Топ предприятий по различным метрикам
- Анализ качества данных
- Гибкая система фильтрации

### 📈 Экспорт и отчётность

- Экспорт в CSV, Excel форматы
- Генерация PDF отчётов с графиками
- Настраиваемые фильтры экспорта
- Автоматическое форматирование данных

## 🏗 Модели данных

### Enterprise (Предприятие)
```python
{
    "id": 1,
    "name": "ООО Предприятие",
    "industry": "Машиностроение",
    "region": "Центральный",
    "employees": 150,
    "revenue": 50000000.0,
    "taxes_paid": 5000000.0,
    "address": "Москва, ул. Примерная, д. 1",
    "phone": "+7-495-123-45-67",
    "email": "info@enterprise.ru",
    "status": "active",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00"
}
```

## 🌍 Поддерживаемые регионы Москвы

- Центральный
- Северный  
- Северо-Восточный
- Восточный
- Юго-Восточный
- Южный
- Юго-Западный
- Западный
- Северо-Западный
- Новомосковский
- Троицкий

## 🏭 Поддерживаемые отрасли

- Машиностроение
- Пищевая промышленность
- Химическая промышленность
- Текстильная промышленность
- Металлургия
- Электроника
- Строительные материалы
- Фармацевтика
- Автомобилестроение
- Полиграфия
- Другое

## 🔐 Переменные окружения

```env
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/enterprises_db

# Application
APP_NAME="Система анализа данных предприятий Москвы"
DEBUG=True
SECRET_KEY=your-secret-key

# CORS
ALLOWED_ORIGINS=["http://localhost:3000", "https://bortongo.github.io"]

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=50MB
```

## 🧪 Тестирование

```bash
# Запуск тестов
pytest

# Запуск с покрытием
pytest --cov=.
```

## 📝 Логирование

Система ведёт подробные логи:
- Загрузка и обработка файлов
- Ошибки валидации данных
- Производительность API
- Ошибки базы данных

## 🚀 Производительность

- Асинхронная обработка запросов
- Оптимизированные SQL запросы
- Пагинация для больших наборов данных
- Кеширование агрегированных данных (Redis)

## 🔒 Безопасность

- Валидация всех входных данных
- Защита от SQL инъекций (SQLAlchemy ORM)
- Ограничение размера загружаемых файлов
- CORS настройки

## 📊 Мониторинг

- Health check эндпоинт: `/health`
- Метрики производительности
- Логирование всех операций

## 🤝 Интеграция с Frontend

Backend полностью совместим с Next.js frontend частью проекта и предоставляет все необходимые API для работы системы анализа данных предприятий Москвы.

## 📞 Поддержка

Для вопросов и предложений создайте issue в GitHub репозитории.
